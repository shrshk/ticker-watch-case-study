# notes/

Working documents from the build: the implementation plan, with the sections
that measurement overturned corrected in place and dated, and the pre-push
code review with every finding and its status. Kept in the repository for the
record; excluded from `make submit` because they describe how the work was
sequenced rather than what was built. The reviewer-facing account is the
top-level README, `docs/measurements.md` and `docs/limitations-and-next-steps.md`.
